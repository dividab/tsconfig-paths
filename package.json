{
  "name": "node-tsconfig-paths",
  "version": "0.2.1",
  "description": "Load node modules at run-time according to tsconfig.json paths.",
  "main": "lib/index.js",
  "author": "Jonas Kello",
  "license": "MIT",
  "repository": "https://github.com/jonaskello/node-tsconfig-paths",
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/mocha": "^2.2.35",
    "@types/node": "^6.0.54",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "shelljs": "^0.7.5",
    "ts-node": "^2.0.0",
    "typescript": "^2.1.4"
  },
  "scripts": {
    "start": "cd src && ts-node index.ts",
    "start:example": "yarn tsc && cd example/node && ts-node -r ../register.js main.ts",
    "tsc": "tsc -P src",
    "tsc:tests": "tsc -P tests",
    "test": "mocha --compilers ts:ts-node/register tests/**/*.ts",
    "build": "rm -rf lib && tsc -P src",
    "publish:major": "yarn run build && node scripts/publish.js major",
    "publish:minor": "yarn run build && node scripts/publish.js minor",
    "publish:patch": "yarn run build && node scripts/publish.js patch"
  },
  "dependencies": {
    "tsconfig": "^5.0.3"
  }
}
